1. Support for conda/mamba instead of VENV
2. Dependency on sudo apt-get install libnss3
3. Make sure to export MIDRC_CREDENTIALS_PATH="./credentials.json"
3. Add fonts with emoji support:
    - sudo apt install fonts-noto-color-emoji
    - fc -cache -f -v